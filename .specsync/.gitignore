# spec-sync v4 — generated by `specsync migrate`
# Committed: config.toml, registry.toml, lifecycle/, changes/, archive/, hashes.json
# Ignored: backups (migration artifact), local config (per-developer AI settings)

backup-3x/
config.local.toml
